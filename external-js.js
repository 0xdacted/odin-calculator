const add = (a,b) => { 
    return parseInt(a + b);
  };

  const subtract = (a,b) => {
	return parseInt(a - b);
};

const multiply = (a,b) => {
    return parseInt(a * b);
    };

const divide = (a,b) => {
    return parseInt(a / b);
    };